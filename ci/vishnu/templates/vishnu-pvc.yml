kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "vishnu.pvc" . }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "aks-managed-retain"
  resources:
    requests:
      storage: 20Gi