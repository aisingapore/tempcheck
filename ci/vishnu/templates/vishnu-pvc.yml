kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "vishnu.pvc" . }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "azurefile-std"
  resources:
    requests:
      storage: 20Gi